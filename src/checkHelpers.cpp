//
// Created by Francesca Damian
//

#include "board.h"

bool board::isCheckmate(const shared_ptr<piece>& king, std::vector<std::vector<shared_ptr<piece>>> &board) const {
return false;
}

bool board::isCheck(short int column, short int row, char pieceName, std::vector<std::vector<std::shared_ptr<piece>>> &board) const {
return false;
}

